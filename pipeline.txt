# Порядок агентов в статическом пайплайне (по строке на агента).
# Это просто имена папок из agents/. Меняй порядок/состав как угодно.
# Основной режим — динамический оркестратор (agents/orchestrator), он сам решает,
# кого звать. Этот файл нужен только для линейного pipeline-режима.
researcher
analyst
writer
