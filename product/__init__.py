"""
Evergreen — продукт «AI-операционист», построенный ПОВЕРХ движка composer/.

Граница (правило одной стрелки):
    product/  --импортирует-->  composer/        ✅
    composer/ --импортирует-->  product/          ❌ НИКОГДА

Движок (composer/) — доменно-независимый харнесс, «мозги»: цикл агента,
провайдеры LLM, инструменты, оркестрация. Он НЕ знает, что существует Evergreen.

Продукт (product/) — всё, что делает из движка конкретный SaaS по ТЗ:
    product/db/       — PostgreSQL: схемы, миграции, доступ к данным.
    product/auth/     — аутентификация и RBAC (Собственник/Управляющий/Сотрудник).
    product/modules/  — бизнес-модули M1..M6 (база знаний, чат, онбординг, ОС, …).
    product/api/      — REST-слой поверх движка и модулей.
    product/brain.py  — ЕДИНСТВЕННЫЙ мост к composer/. Весь продукт зовёт движок
                        только через него — чтобы шов оставался чистым.

Когда появится второй продукт на том же движке (или решим лицензировать харнесс) —
composer/ вынесется в отдельный репозиторий через `git subtree split`. Граница уже
чистая, поэтому это будет дёшево. До тех пор — один репозиторий, два пакета.
"""

__version__ = "0.1.0"
